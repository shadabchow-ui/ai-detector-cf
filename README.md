# AI Text Detector (Cloudflare Pages)

Vite + React + TypeScript UI with a Cloudflare Pages Function API at `POST /api/detect`.

## Local dev
```bash
npm install
npm run dev
```

## Test Pages Functions locally
```bash
npm run build
npm run pages:dev
```
Open: http://localhost:8788
